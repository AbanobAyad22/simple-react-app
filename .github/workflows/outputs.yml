name: output
on: 
    push
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: get code
              uses: actions/checkout@v3
            - name: get ls
              run: ls   